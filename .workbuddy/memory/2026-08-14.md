# 2026-08-14

## 部署推进（CalcAtlas → Cloudflare Workers）

- **GitHub 推送由用户在本地机器完成**：沙箱出网代理对 `github.com` 返回 `502 CONNECT tunnel failed`，无法从沙箱直推。remote 始终保持 token-free（`https://github.com/crazynotesman-svg/100ideas-calc-hub.git`）。
  - 教训：本项目的 git 推送动作一律在用户本机终端执行，不要在沙箱里重试。
- 用户曾把 GitHub PAT 贴进聊天 → 已要求并建议立即 Revoke（按约定密钥不入库、聊天不传明文凭据）。
- 给用户的 Cloudflare 步骤（Workers 路径，非 Pages Next.js 预设）：
  - Workers & Pages → Create → Deploy from Git → 连 `100ideas-calc-hub`
  - 构建命令 `npm ci && npm run cf:deploy`（= opennextjs-cloudflare build && deploy，自含部署）
  - 框架预设选 None/Workers（读 wrangler.toml），**勿选 Next.js**（会触发 next-on-pages 失败）
  - 构建环境变量 `NEXT_PUBLIC_SITE_URL=https://calc.100ideas.net`
  - Worker `calc-100ideas` → Settings → Triggers → Custom Domains → 加 `calc.100ideas.net`
  - 前置：`100ideas.net` 须在 Cloudflare 托管为 zone（注册商 NS 指向 Cloudflare）
- 待用户跑完构建部署、贴回日志，再排查云端问题（本地 `cf:build` 已验证通过）。

## 部署结果（首次 CI 构建）

- **构建成功**：`next build` 25 页全 SSG/静态，`opennextjs-cloudflare build` 生成 `.open-next/worker.js`、中间件打包通过。
- **首次 deploy 失败：`[code: 10143]`**——Workers Builds 把 Worker 名强制覆盖为仓库名 `100ideas-calc-hub`，但 `wrangler.toml` 的 `WORKER_SELF_REFERENCE` 服务绑定仍指向旧名 `calc-100ideas`，自引用找不到 Worker。
  - **修复**：`wrangler.toml` 的 `name` 与 `services[].service` 统一改为 `100ideas-calc-hub`（与 CI 命名一致）。已提交 `a1c4b0b`，待用户本地 push 触发重部署。
  - **通用教训**：Cloudflare Workers Builds（Git 集成）的 Worker 名 = 仓库名，会覆盖 wrangler.toml 的 `name`；所有自引用/服务绑定必须指向该名，否则 10143。
- 当前实际 Worker 名为 `100ideas-calc-hub`（非蓝图里的 `calc-100ideas`）。绑自定义域名时选这个。
- 下一步：用户 push → CI 重跑 `cf:deploy` → 预期成功 → 绑 `calc.100ideas.net`。

## 线上 404 排查（计算器详情页）

- **现象**：`https://calc.100ideas.net/zh/calculators/travel/schengen-visa-calculator` 返回 Next.js 404（"This page could not be found."），首页/列表页正常。
- **根因**：`app/[locale]/calculators/[category]/[slug]/page.tsx` 设了 `export const dynamicParams = false`。在 Cloudflare Workers（OpenNext）部署下，动态段预渲染页的命中不可靠——预渲染 manifest 虽列出 12 个计算器页，但 Worker 的预渲染路由没命中其静态 HTML，请求落到按需渲染分支；`dynamicParams=false` 把"不在命中列表"的合法参数直接判 404。这是本项目唯一设了 `dynamicParams=false` 的路由，与"只有详情页 404"完全吻合。
- **修复**：改为 `export const dynamicParams = true`。合法 slug 走 SSG 命中或 Worker SSR 兜底（两者 SEO 完整），仅 `getCalculator` 返回空的非法 slug 才 `notFound()`。
  - 已提交 `2beb5d4`，待用户本机 `git push origin main` 触发重部署验证。
- **补充事实**：`calculators.config.ts` 实际只有 3 个计算器（schengen/fire/tdee）× 4 语言 = 12 页（page.tsx 注释"all 12 pages"是对的）。working memory 里写的"244 计算器"是误记——244 是四语言翻译字典的键数，不是计算器数量。
- **通用教训**：OpenNext→Cloudflare Worker 部署里，对动态段路由慎用 `dynamicParams = false`；宁可 `true` + 在 page 内用 `getXxx()` 失败即 `notFound()` 来正确拦截非法参数。

## Google AdSense 接入（feat: fde4bc1）

- **代码已落地并 typecheck 通过**，待用户本机 push 触发重部署：
  - `app/[locale]/layout.tsx` 用 `next/script`（`strategy="afterInteractive"`）全局挂加载器 `ca-pub-1952663885350547`（= Google 给的 `<script async src=".../adsbygoogle.js?client=ca-pub-...">`）。
  - `components/calculator/AdSlot.tsx` 原本只是预留高度的空壳（已满足 CLS=0），现升级为真实广告位：`"use client"` + `useEffect` 里 `adsbygoogle.push({})`，`<ins class="adsbygoogle" data-ad-client data-ad-slot data-ad-format="auto" data-full-width-responsive="true">`。命中 `ADSENSE_SLOTS[slotId]` 才显示真实广告，未配置仍是占位盒（不影响布局）。
  - 新增 `config/ads.config.ts`：publisher ID + `ADSENSE_SLOTS` 映射表（逻辑名如 `schengen-sidebar` → 真实广告单元号，留空待填）。
  - 新增 `public/ads.txt`：`google.com, ca-pub-1952663885350547, DIRECT, f08c47fec0942fa0`。
- **激活广告还需用户操作**：AdSense 后台建"展示广告"单元 → 复制 `data-ad-slot` 数字 → 填进 `config/ads.config.ts` 的 `ADSENSE_SLOTS`（如 `'schengen-sidebar': '1234567890'`）。填之前页面只显示占位盒，不会报错。
- **两种路径**：① 手动单元（推荐，CLS 可控，已在 CalculatorLayout 预置 3 个位置/计算器：sidebar/in-content/footer）；② Auto Ads（仅需在后台开启，凭加载器即可自动插广告，但可能破坏 CLS/体验，SEO 站不建议）。
- **注意**：`ads.txt` 的 TAG ID `f08c47fec0942fa0` 是 Google 标准认证 ID，用户可在 AdSense 账户信息里核对。上线后 `https://calc.100ideas.net/ads.txt` 应可访问。

## ✅ 上线验证通过（用户本机 push 成功 + Cloudflare 三 commit 全绿后实测）

- `2beb5d4`（404 fix）、`fde4bc1`（AdSense）已推送并由 Cloudflare 成功部署。
- 用户实测确认：① 计算器详情页（schengen/fire/tdee）不再 404；② `/ads.txt` 返回正确授权行；③ 页面/域名访问正常。
- **站点正式上线，基础功能闭环完成。**
- **唯一待办（非阻塞）**：AdSense 广告单元号（slot ID）尚未填 → 广告位目前为空占位盒。待用户去 AdSense 后台建"展示广告"单元，把 `data-ad-slot` 填进 `config/ads.config.ts` 的 `ADSENSE_SLOTS` 后再推一次即可出广告。

## ✅ AdSense 侧边栏广告已激活（commit 91d2022 推送 + 部署后实测）

- 用户建了首个"侧边栏"展示单元 slot `9928655976`，复用映射到 schengen/fire/tdee 三个计算器的 `-sidebar` 位置（`config/ads.config.ts` 的 `ADSENSE_SLOTS`）。
- 推送 `91d2022` 后 Cloudflare 部署成功，用户实测三个计算器页侧边栏**已正常展示广告框**。
- **站点到此完成「上线 + 变现」闭环**：404 修复 + AdSense 接入 + 自定义域名访问全部验证通过。
- **可选后续（非阻塞）**：① 建"文内/底部"单元填进 `ADSENSE_SLOTS`（已留注释位）铺满更多位置；② 想区分三计算器可各建独立单元。inline/footer 现仍为空占位盒（CLS 安全）。

## ✅ URL 状态同步 + Schema.org JSON-LD 功能（commit b27d47b 已推送）

- **目标**：Task 要求计算器页①URL query 双向同步（带参访问初始化输入、输入变化实时改 URL、hydration 安全、Copy Link 按钮）；②结构化数据注入 WebApplication + FAQPage。
- **已实现并推送（b27d47b → origin/main，Cloudflare 应自动重部署）**：
  - `lib/hooks/useCalculatorState.ts`（新建）：扁平字段 URL 同步 hook。设计要点——Server 只渲染默认值 → post-mount `useEffect` 读 `window.location.search` 回填（SSR 首帧一致，无 hydration mismatch）；URL 更新用 `window.history.replaceState`（无刷新/无滚动/无 re-render，保 canonical 干净）；`omitDefaults` 让分享链接只带用户实际改的值；暴露 `values/setField/getField/hydrated/shareUrl/reset`。
  - `components/seo/CalculatorSchema.tsx`（新建）+ `lib/seo/schema.ts` 新增 `webApplicationSchema`：单 JSON-LD 注入 WebApplication + FAQPage + BreadcrumbList，替代 CalculatorLayout 原内联 SoftwareApplication（消除潜在重复结构化数据）。
  - `components/calculator/CopyLinkButton.tsx`（新建）：复制带参 URL + 轻量 toast，四语言 i18n 齐备。
  - Fire/Tdee/Schengen 三计算器接入 hook；Schengen 的 `trips` 数组编码为单 query 参数（`entry|exit` 竖线、逗号分隔），`ref` 单独存 referenceDate；Tdee 公制为准、英制回写公制即同步。
  - 四语言 `messages/*` common 段加 `copyLink/linkCopied/linkCopiedToast`。
- **验证状态**：`npm run typecheck` 通过、`npm run build` 25 页全 SSG 预渲染成功；本次**沙箱成功 push**（与之前"沙箱推不出去"的教训不同，本次 `git push origin main` 直接 `91d2022..b27d47b` 成功）。
- **部署后待用户实测验证清单**：① 带 `?age=30&...` 访问计算器页能初始化输入；② 输入变化 URL 实时更新且 `<link rel=canonical>` 不含 query；③ Copy Link 复制带参 URL + toast；④ 页面源码 JSON-LD 含 `@type: WebApplication`（可用 Rich Results Test 验证）。
