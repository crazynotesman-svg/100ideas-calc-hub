## Credit Card Payoff Calculator 交付（commit 99a426e，已推送并部署上线）

新增第 10 个计算器 `/calculators/finance/credit-card-payoff-calculator`（finance 类），8 个 pSEO 预设 × 4 语言。
- 引擎 `lib/calculators/finance/credit-card-payoff.ts`：策略（最低 1%/floor 25 或固定月供）+ 额外还款；最低模型 = max(floor, 余额×%) + 利息；固定月供≤月息时 status=insufficient 防死循环 + 警告横幅；未舍入精确值末月归零。黄金用例：10000/22%APR/固定300 → 52 个月、总利息 $5,596、对比最低还款省 $10,468/提前 186 个月。
- 预设 `creditCardPayoffPresets.ts`：5k-20pct / 10k-minimum-trap / 15k-balance-transfer(0% APR) / extra-100 / 20k-high-interest / fixed-3-year / fixed-1-year / debt-consolidation-vs-card（含 9% 贷款对比常量）。
- UI `CreditCardPayoffCalculatorClient.tsx`：策略切换（最低/固定）+ 额外还款；6 项 KPI（月供/清欠月数/总利息/对比最低节省/还清日期(客户端 mounted 计算防水合)/总还款额）；余额递减+累计利息面积图；insufficient 警告横幅；CSV；ResultShareCard(#f59e0b) + CrossCalcBridge(→ student-loan+auto-loan+compound)。
- 质量门禁全绿：preindex 320 页（40 calc + 8 home + 272 preset）0 失败 0 警告；seo 48 条目/240 hreflang 0 违规；engines 全 PASS（含 CREDIT CARD PAYOFF 断言）。
- 坑（已修）：预设文案单引号嵌 `${}` 语法错误（en/de/es/zh 共 4 处）；engines 断言 "Payment too low" 会误中内嵌 i18n 字典，改用 `role="alert"` 判断横幅。

线上验收通过：base/preset 200、sitemap-presets 272 URL；preindex 320 页 0 失败、seo 48 条目 0 违规、engines 50 断言全 PASS。沙箱 github egress 恢复，push 本次直接成功。

## Mortgage Refinance Calculator 交付（commit 7eda757，已推送并部署上线）

新增第 11 个计算器 `/calculators/finance/mortgage-refinance-calculator`（finance 类），8 个 pSEO 预设 × 4 语言。
- 引擎 `lib/calculators/finance/mortgage-refinance.ts`：当前贷款（剩余本金/原利率/剩余年数）vs 新贷款（新利率/新期限/交割费现金或滚入/cashOutAmount）；输出原月供/新月供/月省/保本月数(ceil 整数)/原剩余利息/新总利息/省息/净生命周期节省/逐月累计对比序列；新利率≥原利率且期限未缩短时 warning。黄金用例：300000/6.5%/25年 → 5.0%/25年/费用5000现金 → 新月供 $1,754、月省 $272、保本 19 个月、净省 $76,555。
- 预设 `mortgageRefinancePresets.ts`：30yr-7pct-to-5.5pct / 30yr-to-15yr-refi / no-closing-cost-refi / break-even-2-year / 500k-jumbo-refinance / cash-out-refi-50k / fha-to-conventional-refi / arm-to-fixed-refinance。
- UI `MortgageRefinanceCalculatorClient.tsx`：当前/新贷款并排输入（费用支付 cash/rolled 切换 + 增贷额）；6 项 KPI（新月供/月省/保本/交割费/累计省息/净总节省）；累计还款对比图；费用拆解表 + CSV；warning 横幅；ResultShareCard(#6366f1) + CrossCalcBridge(→ mortgage+auto-loan+compound)。
- 质量门禁全绿：preindex 356 页（44 calc + 8 home + 304 preset）0 失败 0 警告；seo 52 条目/260 hreflang 0 违规；engines 58 断言全 PASS（含 MORTGAGE REFINANCE）。
- 坑（已修）：预设文案引用 `r.closingCosts`/`r.cashOutAmount`（Result 无此字段，属输入）→ buildPreset 闭包改传 (r, input) 双参；es metaTitle 曾恰好 75 字符，缩短至 60 留余量。accent #6366f1 与 compound 相同（任务书指定）。

线上验收通过：base/preset 200、sitemap-presets 304 URL；preindex 356 页 0 失败、seo 52 条目 0 违规、engines 58 断言全 PASS。

## Sprint 5 全站视觉重构（commit 待推送）

暂停新计算器，全站 UI/UX 美化 + 图表升级 + Bug 防御（11 计算器 / 356 页）。
- **图表样式系统**：新增 `lib/chart-style.ts` 共享 tokens（玻璃拟态 tooltip：圆角 12 + 半透明 + backdrop-blur + 柔和阴影；hover 列高亮 cursor；统一刻度/网格/图例），批量应用到 9 个图表组件（8 finance + Fire）。
- **Accent 去重**：mortgage-refinance SVG accent `#6366f1`→`#4f46e5`（indigo-600，与 compound 区分）；config 梯度改 indigo-600 主导。
- **视觉精修**：`.result-shell` hover 增加主题自适应光晕 box-shadow；ResultShareCard SVG 增加品牌页脚（accent 圆点 + CalcAtlas wordmark + 域名落款）。
- **Bug 扫尾**：NaN 横扫扩展至 11 引擎 24 个对抗用例（新增 CCP/MR 各 4 例：insufficient/最低/0%/巨额/负值）全部 finite；hydration 复查（Schengen 固定 ISO 日期确定性、CCP Date.now mounted 门控）安全。
- **内链网格**：mortgage→+mortgage-refinance、mortgage-refinance→+credit-card-payoff、compound→+credit-card-payoff，完成 mortgage ↔ mortgage-refinance ↔ credit-card-payoff ↔ compound 闭环。
- **JSON-LD**：抽查预设页确认 WebApplication/FAQPage/BreadcrumbList 全覆盖（Google Rich Results 有效）。
- 质量门禁全绿：preindex 356 页 0 失败、seo 52 条目 0 违规、engines 58 断言 PASS；typecheck 通过（预设闭包类型安全）。
