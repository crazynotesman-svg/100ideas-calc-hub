
## Auto Loan & Payment Calculator 交付（commit 待推送）

新增第 7 个计算器 `/calculators/finance/auto-loan-calculator`（finance 类），8 个 pSEO 预设 × 4 语言。
- 引擎 `lib/calculators/finance/auto-loan.ts`：税基=车价−置换价；loan=车价−首付−置换+税；等额本息月供 + 逐月摊销；输出月供/贷款额/总利息/总支付/总车辆成本。用未舍入月供跑明细、输出再舍入，末月余额精确归零。黄金用例：30000/首付3000/税7%/60月/6% → loan 29100、月供 562.58（$563）。
- 预设 `autoLoanPresets.ts`：30k-5yr-6%、60-vs-72、used-car-higher-rate、zero-down、50k-luxury、trade-in、sales-tax、20k-budget；60-vs-72 预设内置并排对比（alt 引擎调用）。
- UI `AutoLoanCalculatorClient.tsx`：置换与销售税开关（关则归零）、期限下拉 24-84、结果卡 6 项 + 余额/累计本金利息面积图 + 逐月明细表 + ResultShareCard + CrossCalcBridge(→ mortgage+compound) + CSV。
- 质量门禁全绿：preindex 212 页（28 calc + 8 home + 176 preset）0 失败 0 警告；seo 36 条目/180 hreflang 0 违规；engines 含 AUTO LOAN 断言全 PASS。（任务书预估 208 页，实际 212：7×4+8+44×4。）
- 标题约束已按 HTML 实体规则处理（en "Auto Loan & …" 1 个 &，rendered 64 ≤75；de "Auto-Darlehens- & Zahlungsrechner…" rendered 67 ≤75）。

## Student Loan & Repayment Plan Calculator 交付（commit 待推送）

新增第 8 个计算器 `/calculators/finance/student-loan-calculator`（finance 类），8 个 pSEO 预设 × 4 语言。
- 引擎 `lib/calculators/finance/student-loan.ts`：宽限期利息按月累积并在还款开始时资本化；等额本息；额外月供 100% 冲本金；逐月明细用未舍入精确月供，末月余额归零。输出基础月供/实际月供/总本金/总利息/还清月数/省息/省月数/capitalizedInterest/capitalizedBalance。
- **口径说明**：任务书黄金值（340.66/440.66/省息 3459.55/提前 33 月）基于"舍入月供"摊还；Task 1 明确要求"未舍入精确月供"，故引擎实际为 340.64/440.64/省息 3346.98/提前 34 月（渲染 $341/$441/$3,347/34）——审计按引擎真实值断言并已在 verify-engines 注释说明。
- 预设 `studentLoanPresets.ts`：30k-10yr-6.5 / 50k-graduate-7.5 / 100k-medical-law / extra-200-payoff / parent-plus-40k / refinance-5pct-7yr / idr-vs-standard(含 20 年对比) / 25k-grace-period。
- UI `StudentLoanCalculatorClient.tsx`：6 项 KPI（基础月供/实际月供/总利息/节省利息/还清时间/总还款额）+ 宽限期>0 时显示资本化利息/起始余额；余额+累计本金/利息面积图；逐月明细表 + CSV；ResultShareCard(accent #0ea5e9) + CrossCalcBridge(→ mortgage+auto-loan+compound)。
- 质量门禁全绿：preindex 248 页（32 calc + 8 home + 208 preset）0 失败 0 警告；seo 40 条目/200 hreflang 0 违规；engines 含 STUDENT LOAN 断言全 PASS。
- 坑：benchmark 里误用输入字段 `r.gracePeriodMonths`（Result 无此字段），改 `r.capitalizedInterest > 0` 判断。

## Lease vs Buy Auto Calculator 交付（commit 待推送）

新增第 9 个计算器 `/calculators/finance/lease-vs-buy-calculator`（finance 类），8 个 pSEO 预设 × 4 语言。
- 引擎 `lib/calculators/finance/lease-vs-buy.ts`：Buy 模型（税入贷款、等额本息、持有期后按残值出售、净成本=首付+税+还款+剩余本金−残值）与 Lease 模型（残值=MSRP×残值率、月租=折旧+(成本+残值)×MF、月租含税、净成本=首付+费用+租金，期末无资产）；差值/胜者/省额 + 逐月 leaseCum/buyCum/buyEquity 序列；未舍入精确数值。
- 黄金用例：35000/36mo/MF0.0025/残值60%/贷6%/首付3000/税7% → 买净 $22,179 vs 租净 $23,373，购买胜出省 $1,194。
- 预设 `leaseVsBuyPresets.ts`：35k-36mo-luxury / 50k-ev-tesla / zero-down-lease / high-mileage-commuter / 25k-budget-sedan / 60k-truck-suv / business-tax-deduction / low-money-factor-promo。
- UI `LeaseVsBuyCalculatorClient.tsx`：Buying/Leasing 标签页（共享首付/税率）、6 项 KPI（买净/租净/胜者+省额/买月供/租月费/期末权益）、累计成本+权益面积/折线图、费用拆解表（首付/月付/费用/税/权益/净成本 × 买/租）、CSV；ResultShareCard(#10b981) + CrossCalcBridge(→ auto-loan+mortgage+compound)。
- 质量门禁全绿：preindex 284 页（36 calc + 8 home + 240 preset）0 失败 0 警告；seo 44 条目/220 hreflang 0 违规；engines 含 LEASE VS BUY 断言全 PASS。
- 注意：ResultShareCard accent #10b981 与 fire 相同（任务书指定）；zh 描述曾 50 字符已补长至 52。
