# 2026-08-18 工作日志

## 复利计算器引擎 + pSEO 预设矩阵（交付，commit c485b48 → push origin main）

完成 Task：Compound Interest & Investment Growth Calculator (`/calculators/finance/compound-interest-calculator`)。

关键交付物：
- `lib/calculators/finance/compound.ts`：纯函数引擎（P、月定投 PMT、年化 r%、年限 t、复利频率 n，默认月）。day-number 安全、无 Date。
- `components/calculators/finance/CompoundInterestCalculatorClient.tsx`：输入 + 结果徽章 + 逐年明细表/面积图 + `ResultShareCard` + `CrossCalcBridge`（指向 fire）+ CSV 导出。
- `app/.../preset/compoundInterestPresets.ts`：8 个长尾预设（引擎数值注入 FAQ）。
- 路由挂载：`[slug]/page.tsx`（registry + bottomSlot）、`preset/[scenario]/page.tsx`（GSP + Benchmark）、`sitemap-presets.xml`、`config/calculators.config.ts`（id=compound）。
- 四语言 i18n（en/de/es/zh）。

修复（关键）：
- `lib/hooks/useCalculatorState.ts` SSR 守卫：`writeUrl`/`shareUrl` 内的 `window` 访问改为「client 才执行」（用 `typeof window` 判断）。此前所有计算器（schengen/fire/tdee/compound）预渲染都崩在 `window is not defined`——这是既存缺陷，非新代码引入。
- es `metaTitle` 74→56 字符、zh `metaDescription` 38→58 字符，满足审计长度规则（标题渲染值 = i18n + ` | CalcAtlas` 12 字符；描述 >50）。

验证（全绿）：
- `npm run build`：110 静态页（36 个复利：4 base + 8 预设 × 4 语言），exit 0。
- `audit:preindex`：**PASS**（104 页，0 失败）。
- `audit:seo`：**PASS**（0 违规，扩展脚本覆盖 compound 后暴露并修复两个真实 SEO 缺陷）。
- `audit:engines`：**PASS**（无回归）。

经验沉淀：
- 审计脚本 `preindex-check.mjs` / `seo-audit.mjs` 的路由清单是硬编码的，新增计算器必须同步扩展，否则审计静默通过却不校验新页面。已写入 MEMORY.md 质量门禁。
- 判断 build 成败只看 `$?`，不要信管道 `tail`（会吞非零退出码）。
